# Configuración para desarrollo local
spring.datasource.url=${DB_URL:jdbc:mysql://michibase.chqru7wd0fnl.us-east-1.rds.amazonaws.com:3306/meowtfit}
# Cambia el valor de DB_USER y DB_PASS en tu entorno local si es necesario
spring.datasource.username=${DB_USER:michiadmin}
spring.datasource.password=${DB_PASS:SanchezDeidad123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.open-in-view=false

# Origen permitido para CORS
cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173}

# Configuración de carga de archivos
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Directorio donde se guardan los comprobantes de pago
app.upload.comprobantes-dir=${UPLOAD_DIR:uploads/comprobantes}